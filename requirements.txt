# 예약 발행 스크립트(auto_publish_with_images.py)에서 퍼즐 이미지 생성 시 필요 (둘 중 하나만 있어도 됨)
Pillow>=9.0.0

# 대안: Pillow 없이 play.html 인쇄 레이아웃으로 PNG 저장 (실제 퍼즐 그리드)
# pip install playwright && playwright install chromium
# playwright>=1.40.0
